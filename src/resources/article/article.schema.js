const jsonSchema = {
  practice: [String],
  articleTitle: {
    type: String,
  },
  author: [String],
  source: String,
  year: Number,
  doi: String,
  benefits: [String],
};

export default jsonSchema;
